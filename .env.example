# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# Firebase Configuration
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# ===================================================================
# ユーザーフィードバック収集（docs/FEEDBACK_SETUP.md 参照）
# ===================================================================
# 未設定でも動作します（その場合は Firestore の feedback コレクションのみに保存）。
#
# VITE_FEEDBACK_WEBHOOK_URL:
#   Google Apps Script（docs/feedback-gas.js）のウェブアプリURL。
#   設定すると Google スプレッドシートへの追記／メール通知が有効になります。
VITE_FEEDBACK_WEBHOOK_URL=
#
# VITE_FEEDBACK_EMAIL:
#   通信失敗時に提示する mailto: の宛先。未設定なら mntobira@gmail.com。
VITE_FEEDBACK_EMAIL=
#
# VITE_APP_VERSION:
#   フィードバックに添付するアプリのバージョン表記（不具合の切り分け用）。
VITE_APP_VERSION=
