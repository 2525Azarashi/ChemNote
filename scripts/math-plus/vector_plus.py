# -*- coding: utf-8 -*-
"""ベクトル 入試レベル強化問題（sympy 検算付き）"""
from harness import *
from sympy import Matrix, sqrt, Rational as R, acos, pi, symbols, solve, simplify, Abs, diff, det

def V(*c): return Matrix(c)
def dot(u,v): return (u.T*v)[0]
def norm(u): return sqrt(dot(u,u))
def area(u,v): return sqrt(dot(u,u)*dot(v,v)-dot(u,v)**2)/2
s_, t_, u_ = symbols('s_ t_ u_')

# ---------------------------------------------------------------------
# mv_1 演算・成分・大きさ
# ---------------------------------------------------------------------
P('q_mv_1_plus_comp', 'mv_1', '成分計算と |a + tb| の最小', """
a = (2, -1), b = (1, 3) とする。

（1）3a - 2b を成分で表せ。
（2）|3a - 2b| を求めよ。
（3）|a + t b| が最小となる実数 t の値を求めよ。
（4）（3）のときの |a + t b| の最小値を求めよ。
""", [
 dict(id='q_mv_1_plus_comp_1', label='（1）3a - 2b', answer='(4, -9)', accepted=['(4,-9)','（4, -9）'],
      check=lambda: 3*V(2,-1)-2*V(1,3) == V(4,-9)),
 dict(id='q_mv_1_plus_comp_2', label='（2）|3a - 2b|', answer='√97', accepted=['√(97)','ルート97'],
      check=lambda: norm(V(4,-9)) == sqrt(97)),
 dict(id='q_mv_1_plus_comp_3', label='（3）t', answer='1/10', accepted=['t = 1/10','t=1/10','0.1'],
      check=lambda: solve(diff(dot(V(2,-1)+t*V(1,3),V(2,-1)+t*V(1,3)),t),t) == [R(1,10)]),
 dict(id='q_mv_1_plus_comp_4', label='（4）最小値', answer='7√10/10', accepted=['7√10/10','(7√10)/10','7/√10','(7/10)√10','7√(10)/10','√(49/10)'],
      check=lambda: simplify(norm(V(2,-1)+R(1,10)*V(1,3)) - 7*sqrt(10)/10) == 0),
], """
（1）3a = (6, -3)、2b = (2, 6)。差は (4, -9)。

（2）√(4² + 9²) = √(16 + 81) = √97。

（3）成分で a + tb = (2 + t, -1 + 3t)。
　|a + tb|² = (2 + t)² + (3t - 1)² = 10t² - 2t + 5 = 10(t - 1/10)² + 49/10。
　∴ t = 1/10 で最小。
　【内積で】|a + tb|² = |a|² + 2t(a·b) + t²|b|² = 5 + 2t(-1) + 10t²。同じ式になる（a·b = 2·1 + (-1)·3 = -1）。

（4）最小値は √(49/10) = 7/√10 = 7√10/10。

【ポイント】「|a + tb| の最小」は 2 乗して t の 2 次関数に帰着。幾何的には a + tb ⊥ b のとき最小で、(a + tb)·b = 0 → t = -(a·b)/|b|² = 1/10 とも出せる。
""")

P('q_mv_1_plus_para', 'mv_1', '平行条件・分解・単位ベクトル', """
（1）a = (x, 2), b = (4, x + 2) が平行となる x の値をすべて求めよ。
（2）a = (2, -1), b = (1, 3) のとき、c = (5, 1) を c = s a + t b の形で表す。(s, t) を求めよ。
（3）a = (1, 2) と平行で大きさが 2√5 のベクトルのうち、x 成分が負のものを求めよ。
（4）a = (3, -4) と同じ向きの単位ベクトルを求めよ。
""", [
 dict(id='q_mv_1_plus_para_1', label='（1）x', answer='x = 2, -4', accepted=['2, -4','-4, 2','x = -4, 2','2,-4','-4,2','x=2,-4'],
      check=lambda: sorted(solve(x*(x+2)-8, x)) == [-4, 2]),
 dict(id='q_mv_1_plus_para_2', label='（2）(s, t)', answer='(2, 1)', accepted=['(2,1)','s = 2, t = 1','s=2,t=1','（2, 1）'],
      check=lambda: solve([2*s_+t_-5, -s_+3*t_-1],[s_,t_]) == {s_:2, t_:1}),
 dict(id='q_mv_1_plus_para_3', label='（3）', answer='(-2, -4)', accepted=['(-2,-4)','（-2, -4）'],
      check=lambda: norm(V(-2,-4)) == 2*sqrt(5) and V(-2,-4) == -2*V(1,2)),
 dict(id='q_mv_1_plus_para_4', label='（4）', answer='(3/5, -4/5)', accepted=['(3/5,-4/5)','（3/5, -4/5）','(0.6, -0.8)'],
      check=lambda: V(3,-4)/norm(V(3,-4)) == V(R(3,5),R(-4,5))),
], """
（1）a ∥ b ⇔ x(x + 2) - 2·4 = 0（成分の「たすき掛け」が 0）。x² + 2x - 8 = (x + 4)(x - 2) = 0 → x = 2, -4。
　「a ∥ b ⇔ a = kb」で解いてもよいが、成分の x₁y₂ - x₂y₁ = 0 が最速。

（2）(5, 1) = s(2, -1) + t(1, 3) → 2s + t = 5、-s + 3t = 1。第 2 式から s = 3t - 1 を第 1 式に代入：6t - 2 + t = 5 → t = 1、s = 2。∴ (s, t) = (2, 1)。
　平行でない 2 本のベクトルがあれば、平面上の任意のベクトルはただ 1 通りに分解できる（一次独立）。

（3）a に平行 → k(1, 2)。大きさ |k|√5 = 2√5 → k = ±2。x 成分が負 → k = -2、(-2, -4)。

（4）|a| = √(9 + 16) = 5。a/|a| = (3/5, -4/5)。
""")

# ---------------------------------------------------------------------
# mv_2 内積・なす角・垂直
# ---------------------------------------------------------------------
P('q_mv_2_plus_angle', 'mv_2', '内積の条件から大きさ・角を求める', """
|a| = 2, |b| = 3, a·b = -3 とする。

（1）a と b のなす角 θ を求めよ（0° ≦ θ ≦ 180°）。
（2）|a + b| を求めよ。
（3）|2a - b| を求めよ。
（4）a + b と a - b のなす角の余弦 cosφ を求めよ。
""", [
 dict(id='q_mv_2_plus_angle_1', label='（1）θ', answer='120°', accepted=['120','120度','2π/3'],
      check=lambda: acos(R(-3,6)) == 2*pi/3),
 dict(id='q_mv_2_plus_angle_2', label='（2）|a + b|', answer='√7', accepted=['√(7)','ルート7'],
      check=lambda: sqrt(4+9+2*(-3)) == sqrt(7)),
 dict(id='q_mv_2_plus_angle_3', label='（3）|2a - b|', answer='√37', accepted=['√(37)','ルート37'],
      check=lambda: sqrt(4*4+9-4*(-3)) == sqrt(37)),
 dict(id='q_mv_2_plus_angle_4', label='（4）cosφ', answer='-5/√133', accepted=['-5/√133','-5√133/133','-(5/133)√133','-5/√(133)','-5√(133)/133','-5/(√7√19)'],
      check=lambda: simplify((4-9)/(sqrt(4+9-6)*sqrt(4+9+6)) - (-5/sqrt(133))) == 0),
], """
（1）cosθ = a·b/(|a||b|) = -3/6 = -1/2 → θ = 120°。

（2）|a + b|² = |a|² + 2a·b + |b|² = 4 - 6 + 9 = 7 → |a + b| = √7。

（3）|2a - b|² = 4|a|² - 4a·b + |b|² = 16 + 12 + 9 = 37 → √37。

（4）(a + b)·(a - b) = |a|² - |b|² = 4 - 9 = -5。
　|a + b| = √7、|a - b|² = |a|² - 2a·b + |b|² = 4 + 6 + 9 = 19 → |a - b| = √19。
　cosφ = -5/(√7·√19) = -5/√133（= -5√133/133）。

【ポイント】「|a|, |b|, a·b の 3 つ」が分かれば、a と b の 1 次結合どうしの内積・大きさ・角はすべて計算できる。|a ± b|² の展開を機械的に行うこと。
""")

P('q_mv_2_plus_perp', 'mv_2', '垂直条件・なす角（成分）', """
a = (1, 2), b = (3, -1) とする。

（1）a·b を求めよ。
（2）a + k b と a - b が垂直となる実数 k の値を求めよ。
（3）a と b のなす角 θ について cosθ を求めよ。
（4）c = (x, 1) が a と垂直になる x の値を求めよ。
""", [
 dict(id='q_mv_2_plus_perp_1', label='（1）a·b', answer='1', accepted=[],
      check=lambda: dot(V(1,2),V(3,-1)) == 1),
 dict(id='q_mv_2_plus_perp_2', label='（2）k', answer='4/9', accepted=['k = 4/9','k=4/9'],
      check=lambda: solve(dot(V(1,2)+k*V(3,-1), V(1,2)-V(3,-1)), k) == [R(4,9)]),
 dict(id='q_mv_2_plus_perp_3', label='（3）cosθ', answer='√2/10', accepted=['√2/10','1/(5√2)','(√2)/10','1/5√2','√(2)/10','1/√50'],
      check=lambda: simplify(dot(V(1,2),V(3,-1))/(norm(V(1,2))*norm(V(3,-1))) - sqrt(2)/10) == 0),
 dict(id='q_mv_2_plus_perp_4', label='（4）x', answer='-2', accepted=['x = -2','x=-2'],
      check=lambda: solve(dot(V(x,1),V(1,2)), x) == [-2]),
], """
（1）a·b = 1·3 + 2·(-1) = 1。

（2）(a + kb)·(a - b) = |a|² - a·b + k(a·b) - k|b|² = 5 - 1 + k - 10k = 4 - 9k = 0 → k = 4/9。
　成分で a + kb = (1 + 3k, 2 - k)、a - b = (-2, 3) として -2(1 + 3k) + 3(2 - k) = 4 - 9k としても同じ。

（3）cosθ = 1/(√5·√10) = 1/√50 = 1/(5√2) = √2/10。

（4）c·a = x + 2 = 0 → x = -2。

【垂直の判定】「垂直 ⇔ 内積 0」。(2) のように k を含む場合は展開して 1 次方程式にする。|a|², |b|², a·b を最初にメモしておくと速い。
""")

# ---------------------------------------------------------------------
# mv_3 内分・外分・重心
# ---------------------------------------------------------------------
P('q_mv_3_plus_ratio', 'mv_3', '位置ベクトル（内分・外分・重心）', """
三角形 OAB において OA = a, OB = b とする。

（1）辺 AB を 2:1 に内分する点 P の位置ベクトル OP を a, b で表せ。
（2）辺 AB を 1:3 に外分する点 Q の位置ベクトル OQ を a, b で表せ。
（3）三角形 OAB の重心 G の位置ベクトル OG を a, b で表せ。
（4）三角形 ABP（P は（1）の点）の重心 G' の位置ベクトル OG' を a, b で表せ。
""", [
 dict(id='q_mv_3_plus_ratio_1', label='（1）OP', answer='(a + 2b)/3', accepted=['(a+2b)/3','a/3 + 2b/3','(1/3)a + (2/3)b','1/3a+2/3b'],
      check=lambda: simplify((1*a+2*b)/3 - (a+2*b)/3) == 0),
 dict(id='q_mv_3_plus_ratio_2', label='（2）OQ', answer='(3a - b)/2', accepted=['(3a-b)/2','3a/2 - b/2','(3/2)a - (1/2)b','3/2a-1/2b','-b/2 + 3a/2'],
      check=lambda: simplify((-3*a + 1*b)/(1-3) - (3*a-b)/2) == 0),
 dict(id='q_mv_3_plus_ratio_3', label='（3）OG', answer='(a + b)/3', accepted=['(a+b)/3','a/3 + b/3','(1/3)a + (1/3)b','(1/3)(a + b)'],
      check=lambda: simplify((0+a+b)/3 - (a+b)/3) == 0),
 dict(id='q_mv_3_plus_ratio_4', label='（4）OG\'', answer='(4a + 5b)/9', accepted=['(4a+5b)/9','4a/9 + 5b/9','(4/9)a + (5/9)b','4/9a+5/9b'],
      check=lambda: simplify((a + b + (a+2*b)/3)/3 - (4*a+5*b)/9) == 0),
], """
（1）m:n 内分点は (n·OA + m·OB)/(m + n)。2:1 内分なので (1·a + 2·b)/3 = (a + 2b)/3。「比の数字が反対側にかかる」。

（2）m:n 外分点は (-n·OA + m·OB)/(m - n)。1:3 外分：(-3a + b)/(1 - 3) = (3a - b)/2。
　n を負にした「内分の公式」と覚えると統一できる：(-n·a + m·b)/(m + (-n))。

（3）重心は 3 頂点の位置ベクトルの平均：(0 + a + b)/3 = (a + b)/3。O が原点なので OO = 0。

（4）G' = (OA + OB + OP)/3 = {a + b + (a + 2b)/3}/3 = {(4a + 5b)/3}/3 = (4a + 5b)/9。
　係数の和が 4/9 + 5/9 = 1 になっていることを確認（G' は直線 AB 上にある：P も A, B も AB 上だから当然）。
""")

P('q_mv_3_plus_coord', 'mv_3', '座標での内分・外分・重心・平行四辺形', """
3 点 A(1, 2), B(4, -1), C(-2, 6) について、次の点の座標を求めよ。

（1）線分 AB を 1:2 に内分する点 P
（2）線分 AB を 3:1 に外分する点 Q
（3）三角形 ABC の重心 G
（4）四角形 ABCD が平行四辺形となる点 D
""", [
 dict(id='q_mv_3_plus_coord_1', label='（1）P', answer='(2, 1)', accepted=['(2,1)','（2, 1）'],
      check=lambda: (2*V(1,2)+1*V(4,-1))/3 == V(2,1)),
 dict(id='q_mv_3_plus_coord_2', label='（2）Q', answer='(11/2, -5/2)', accepted=['(11/2,-5/2)','（11/2, -5/2）','(5.5, -2.5)','(5.5,-2.5)'],
      check=lambda: (-1*V(1,2)+3*V(4,-1))/(3-1) == V(R(11,2),R(-5,2))),
 dict(id='q_mv_3_plus_coord_3', label='（3）G', answer='(1, 7/3)', accepted=['(1,7/3)','（1, 7/3）'],
      check=lambda: (V(1,2)+V(4,-1)+V(-2,6))/3 == V(1,R(7,3))),
 dict(id='q_mv_3_plus_coord_4', label='（4）D', answer='(-5, 9)', accepted=['(-5,9)','（-5, 9）'],
      check=lambda: V(1,2)+V(-2,6)-V(4,-1) == V(-5,9)),
], """
（1）1:2 内分：(2·A + 1·B)/3 = ((2 + 4)/3, (4 - 1)/3) = (2, 1)。

（2）3:1 外分：(-1·A + 3·B)/(3 - 1) = ((-1 + 12)/2, (-2 - 3)/2) = (11/2, -5/2)。
　外分点は「B を越えた先」にある。B(4, -1) から見て A の反対側に進んだ位置になっているかを図で確認。

（3）G = ((1 + 4 - 2)/3, (2 - 1 + 6)/3) = (1, 7/3)。

（4）平行四辺形 ABCD（頂点がこの順）⇔ AD = BC ⇔ D = A + C - B = (1 - 4 - 2, 2 + 6 + 1) = (-5, 9)。
　「対角線の中点が一致（A + C = B + D）」と覚えると 1 行で出る。
""")

# ---------------------------------------------------------------------
# mv_4 交点・共線条件
# ---------------------------------------------------------------------
P('q_mv_4_plus_cross', 'mv_4', '2直線の交点（係数比較）', """
三角形 OAB において、辺 OA を 2:1 に内分する点を M、辺 OB を 1:2 に内分する点を N とし、線分 AN と BM の交点を P とする。OA = a, OB = b とする。

（1）OP を a, b で表せ。
（2）AP:PN を求めよ。
（3）BP:PM を求めよ。
（4）直線 OP と辺 AB の交点を Q とするとき、AQ:QB を求めよ。
""", [
 dict(id='q_mv_4_plus_cross_1', label='（1）OP', answer='(4a + b)/7', accepted=['(4a+b)/7','4a/7 + b/7','(4/7)a + (1/7)b','4/7a+1/7b'],
      check=lambda: solve([1-s_-R(2,3)*t_, s_/3-(1-t_)],[s_,t_]) == {s_:R(3,7), t_:R(6,7)}),
 dict(id='q_mv_4_plus_cross_2', label='（2）AP:PN', answer='3:4', accepted=['3 : 4','3：4'],
      check=lambda: R(3,7)/(1-R(3,7)) == R(3,4)),
 dict(id='q_mv_4_plus_cross_3', label='（3）BP:PM', answer='6:1', accepted=['6 : 1','6：1'],
      check=lambda: R(6,7)/(1-R(6,7)) == 6),
 dict(id='q_mv_4_plus_cross_4', label='（4）AQ:QB', answer='1:4', accepted=['1 : 4','1：4'],
      check=lambda: (R(4,7)*R(7,5), R(1,7)*R(7,5)) == (R(4,5), R(1,5))),
], """
OM = (2/3)a、ON = (1/3)b。

（1）P は AN 上：AP:PN = s:(1 - s) として OP = (1 - s)a + s·(1/3)b。
　P は BM 上：BP:PM = t:(1 - t) として OP = t·(2/3)a + (1 - t)b。
　a, b は一次独立なので係数比較：1 - s = 2t/3、s/3 = 1 - t。
　第 2 式から s = 3 - 3t、第 1 式に代入：1 - 3 + 3t = 2t/3 → (7/3)t = 2 → t = 6/7、s = 3/7。
　∴ OP = (4/7)a + (1/7)b = (4a + b)/7。

（2）AP:PN = s:(1 - s) = 3/7 : 4/7 = 3:4。

（3）BP:PM = t:(1 - t) = 6/7 : 1/7 = 6:1。

（4）Q は直線 OP 上：OQ = k·OP = (4k/7)a + (k/7)b。Q は AB 上なので係数の和が 1：5k/7 = 1 → k = 7/5。
　OQ = (4/5)a + (1/5)b。内分点の公式 (n·a + m·b)/(m + n) と比べて m = 1, n = 4 → AQ:QB = 1:4。

【別解：メネラウスの定理】三角形 OAN と直線 BM で (OM/MA)(AP/PN)(NB/BO) = 1 → (2/1)(AP/PN)(2/3) = 1 → AP/PN = 3/4。係数比較の検算に使える。
""")

P('q_mv_4_plus_line', 'mv_4', '共線条件 s + t = 1 の使い方', """
三角形 OAB において OA = a, OB = b とする。

（1）OP = (2a + 3b)/5 のとき、点 P は辺 AB 上にある。AP:PB を求めよ。
（2）OP = x a + (2x - 1) b が直線 AB 上にあるとき、x の値を求めよ。
（3）OP = a/3 + b/2 のとき、直線 OP と直線 AB の交点 Q について OQ を a, b で表せ。
（4）（3）のとき、OP:PQ を求めよ。
""", [
 dict(id='q_mv_4_plus_line_1', label='（1）AP:PB', answer='3:2', accepted=['3 : 2','3：2'],
      check=lambda: R(2,5)+R(3,5) == 1),
 dict(id='q_mv_4_plus_line_2', label='（2）x', answer='2/3', accepted=['x = 2/3','x=2/3'],
      check=lambda: solve(x + (2*x-1) - 1, x) == [R(2,3)]),
 dict(id='q_mv_4_plus_line_3', label='（3）OQ', answer='(2a + 3b)/5', accepted=['(2a+3b)/5','2a/5 + 3b/5','(2/5)a + (3/5)b','2/5a+3/5b'],
      check=lambda: (R(6,5)/3, R(6,5)/2) == (R(2,5), R(3,5))),
 dict(id='q_mv_4_plus_line_4', label='（4）OP:PQ', answer='5:1', accepted=['5 : 1','5：1'],
      check=lambda: R(6,5) - 1 == R(1,5)),
], """
【共線条件】点 P が直線 AB 上 ⇔ OP = s a + t b で s + t = 1。（さらに s, t ≧ 0 なら線分 AB 上。）

（1）(2a + 3b)/5 = (2/5)a + (3/5)b。係数の和は 1 なので確かに AB 上。内分点の公式 (n·a + m·b)/(m + n) と比べて m = 3, n = 2 → AP:PB = 3:2。
　「b の係数が AP 側の比」：b の係数 3/5 = AP/AB。

（2）x + (2x - 1) = 1 → 3x = 2 → x = 2/3。

（3）Q は直線 OP 上なので OQ = k·OP = (k/3)a + (k/2)b。AB 上なので k/3 + k/2 = 1 → (5/6)k = 1 → k = 6/5。
　OQ = (2/5)a + (3/5)b = (2a + 3b)/5。

（4）OQ = (6/5)OP なので OP:OQ = 5:6、OP:PQ = 5:1。

【ポイント】「係数の和が 1 になるように定数倍する」のが交点を求める最速手順。(1) の点 P と (3) の点 Q が同じ位置になることにも注目。
""")

# ---------------------------------------------------------------------
# mv_5 面積・正射影
# ---------------------------------------------------------------------
P('q_mv_5_plus_area', 'mv_5', '面積公式と正射影ベクトル', """
|a| = 3, |b| = 2, a·b = 3 とし、OA = a, OB = b とする。

（1）a と b のなす角 θ について cosθ を求めよ。
（2）三角形 OAB の面積 S を求めよ。
（3）b の a への正射影ベクトルを a で表せ。
（4）点 B から直線 OA に下ろした垂線の長さ h を求めよ。
""", [
 dict(id='q_mv_5_plus_area_1', label='（1）cosθ', answer='1/2', accepted=['(1/2)','0.5'],
      check=lambda: R(3, 3*2) == R(1,2)),
 dict(id='q_mv_5_plus_area_2', label='（2）S', answer='3√3/2', accepted=['3√3/2','(3√3)/2','(3/2)√3','3√(3)/2','3/2√3'],
      check=lambda: simplify(sqrt(9*4-9)/2 - 3*sqrt(3)/2) == 0),
 dict(id='q_mv_5_plus_area_3', label='（3）正射影ベクトル', answer='a/3', accepted=['(1/3)a','1/3a','a/3'],
      check=lambda: R(3,9) == R(1,3)),
 dict(id='q_mv_5_plus_area_4', label='（4）h', answer='√3', accepted=['√(3)','ルート3'],
      check=lambda: simplify(2*(3*sqrt(3)/2)/3 - sqrt(3)) == 0),
], """
（1）cosθ = a·b/(|a||b|) = 3/6 = 1/2（θ = 60°）。

（2）面積公式 S = (1/2)√(|a|²|b|² - (a·b)²) = (1/2)√(36 - 9) = (1/2)√27 = 3√3/2。
　（(1/2)|a||b|sinθ = (1/2)·3·2·(√3/2) = 3√3/2 でも同じ。）

（3）b の a への正射影ベクトル = (a·b/|a|²)a = (3/9)a = a/3。
　「b の a 方向成分」を a の何倍かで表す。|a| で割るのではなく |a|² で割る点に注意。

（4）h = |b|sinθ = 2·(√3/2) = √3。あるいは S = (1/2)|a|h → h = 2S/|a| = 3√3/3 = √3。
　h² = |b|² - |正射影|² = 4 - 1 = 3 でも検算できる（正射影の大きさは |a|/3 = 1）。
""")

P('q_mv_5_plus_coord', 'mv_5', '座標での面積・垂線の足', """
3 点 A(1, 1), B(4, 2), C(2, 5) について、次を求めよ。

（1）AB·AC
（2）三角形 ABC の面積 S
（3）AC の AB への正射影ベクトル（成分で）
（4）点 C から直線 AB に下ろした垂線の足 H の座標
""", [
 dict(id='q_mv_5_plus_coord_1', label='（1）AB·AC', answer='7', accepted=[],
      check=lambda: dot(V(3,1),V(1,4)) == 7),
 dict(id='q_mv_5_plus_coord_2', label='（2）S', answer='11/2', accepted=['(11/2)','5.5'],
      check=lambda: area(V(3,1),V(1,4)) == R(11,2)),
 dict(id='q_mv_5_plus_coord_3', label='（3）正射影ベクトル', answer='(21/10, 7/10)', accepted=['(21/10,7/10)','（21/10, 7/10）','(7/10)(3, 1)','(2.1, 0.7)','(2.1,0.7)'],
      check=lambda: R(7,10)*V(3,1) == V(R(21,10),R(7,10))),
 dict(id='q_mv_5_plus_coord_4', label='（4）H', answer='(31/10, 17/10)', accepted=['(31/10,17/10)','（31/10, 17/10）','(3.1, 1.7)','(3.1,1.7)'],
      check=lambda: V(1,1)+R(7,10)*V(3,1) == V(R(31,10),R(17,10)) and dot(V(2,5)-(V(1,1)+R(7,10)*V(3,1)), V(3,1)) == 0),
], """
AB = (3, 1)、AC = (1, 4)。

（1）AB·AC = 3·1 + 1·4 = 7。

（2）成分の面積公式 S = (1/2)|x₁y₂ - x₂y₁| = (1/2)|3·4 - 1·1| = 11/2。
　（√ の公式でも (1/2)√(10·17 - 49) = (1/2)√121 = 11/2。）

（3）(AB·AC/|AB|²)AB = (7/10)(3, 1) = (21/10, 7/10)。

（4）H = A + (正射影ベクトル) = (1 + 21/10, 1 + 7/10) = (31/10, 17/10)。
　検算：CH = (31/10 - 2, 17/10 - 5) = (11/10, -33/10) = (11/10)(1, -3)。AB = (3, 1) との内積は (11/10)(3 - 3) = 0 で確かに垂直。
　CH の長さ = (11/10)√10、S = (1/2)|AB|·CH = (1/2)√10·(11√10/10) = 11/2 で (2) と一致。

【定番手順】垂線の足 = 始点 + 正射影ベクトル。三角形の高さ・距離・対称点はすべてこの手順から派生する。
""")

# ---------------------------------------------------------------------
# mv_6 直線・円のベクトル方程式
# ---------------------------------------------------------------------
P('q_mv_6_plus_line', 'mv_6', '直線のベクトル方程式と点と直線の距離', """
点 A(1, 2) を通り、方向ベクトル d = (3, -1) に平行な直線を ℓ とする。

（1）ℓ の方程式を ax + by + c = 0 の形で求めよ。
（2）点 A を通り、法線ベクトル n = (2, 5) をもつ直線の方程式を求めよ。
（3）ℓ 上の点で原点 O に最も近い点 H の座標を求めよ。
（4）原点 O と ℓ の距離を求めよ。
""", [
 dict(id='q_mv_6_plus_line_1', label='（1）ℓ', answer='x + 3y - 7 = 0', accepted=['x+3y-7=0','x + 3y = 7','x+3y=7','y = -x/3 + 7/3','y = -(1/3)x + 7/3'],
      check=lambda: expand((1+3*t) + 3*(2-t) - 7) == 0),
 dict(id='q_mv_6_plus_line_2', label='（2）', answer='2x + 5y - 12 = 0', accepted=['2x+5y-12=0','2x + 5y = 12','2x+5y=12'],
      check=lambda: 2*1+5*2-12 == 0),
 dict(id='q_mv_6_plus_line_3', label='（3）H', answer='(7/10, 21/10)', accepted=['(7/10,21/10)','（7/10, 21/10）','(0.7, 2.1)','(0.7,2.1)'],
      check=lambda: V(1+3*R(-1,10), 2-R(-1,10)) == V(R(7,10),R(21,10)) and dot(V(R(7,10),R(21,10)), V(3,-1)) == 0),
 dict(id='q_mv_6_plus_line_4', label='（4）距離', answer='7√10/10', accepted=['7√10/10','(7√10)/10','7/√10','(7/10)√10','7√(10)/10'],
      check=lambda: simplify(norm(V(R(7,10),R(21,10))) - 7*sqrt(10)/10) == 0 and simplify(7/sqrt(10) - 7*sqrt(10)/10) == 0),
], """
（1）媒介変数表示：(x, y) = (1, 2) + t(3, -1) → x = 1 + 3t、y = 2 - t。
　t = 2 - y を代入：x = 1 + 3(2 - y) = 7 - 3y → x + 3y - 7 = 0。
　（方向 (3, -1) ⇒ 法線 (1, 3) なので 1(x - 1) + 3(y - 2) = 0 としても速い。）

（2）法線ベクトル n = (2, 5) の直線：n·(p - a) = 0 → 2(x - 1) + 5(y - 2) = 0 → 2x + 5y - 12 = 0。
　「法線ベクトル (a, b) ⇔ 直線 ax + by + c = 0」の対応を即答できるようにする。

（3）H = (1 + 3t, 2 - t) とおき、OH ⊥ d：(1 + 3t)·3 + (2 - t)(-1) = 1 + 10t = 0 → t = -1/10。
　H = (1 - 3/10, 2 + 1/10) = (7/10, 21/10)。

（4）|OH| = √(49 + 441)/10 = √490/10 = 7√10/10。
　点と直線の距離の公式 |c|/√(a² + b²) = 7/√10 = 7√10/10 と一致。「垂線の足を求める → 距離」の流れと公式の両方で検算できる。
""")

P('q_mv_6_plus_circle', 'mv_6', '円のベクトル方程式（直径・接線）', """
（1）2 点 A(1, 2), B(5, -4) を直径の両端とする円の方程式を求めよ。
（2）（1）の円上の点 P(5, 2) における接線の方程式を求めよ。
（3）位置ベクトル p が p·(p - 2c) = 0（c = (1, 3)）を満たす点 P の軌跡は円である。その中心の座標を求めよ。
（4）（3）の円の半径を求めよ。
""", [
 dict(id='q_mv_6_plus_circle_1', label='（1）', answer='(x - 3)^2 + (y + 1)^2 = 13', accepted=['(x-3)^2+(y+1)^2=13','x^2 + y^2 - 6x + 2y - 3 = 0','x^2+y^2-6x+2y-3=0'],
      check=lambda: (V(1,2)+V(5,-4))/2 == V(3,-1) and (norm(V(5,-4)-V(1,2))/2)**2 == 13),
 dict(id='q_mv_6_plus_circle_2', label='（2）接線', answer='2x + 3y - 16 = 0', accepted=['2x+3y-16=0','2x + 3y = 16','2x+3y=16'],
      check=lambda: (5-3)**2+(2+1)**2 == 13 and 2*5+3*2-16 == 0 and V(2,3) == V(5,2)-V(3,-1)),
 dict(id='q_mv_6_plus_circle_3', label='（3）中心', answer='(1, 3)', accepted=['(1,3)','（1, 3）'],
      check=lambda: expand(dot(V(x,symbols('y')), V(x,symbols('y'))-2*V(1,3)) - ((x-1)**2+(symbols('y')-3)**2-10)) == 0),
 dict(id='q_mv_6_plus_circle_4', label='（4）半径', answer='√10', accepted=['√(10)','ルート10'],
      check=lambda: norm(V(1,3)) == sqrt(10)),
], """
（1）直径の両端が A, B の円：(p - a)·(p - b) = 0（円周角 90°）。
　中心は AB の中点 (3, -1)、半径は |AB|/2 = √(16 + 36)/2 = √52/2 = √13。∴ (x - 3)² + (y + 1)² = 13。
　（(p - a)·(p - b) = 0 を成分で展開すると (x - 1)(x - 5) + (y - 2)(y + 4) = 0 → x² + y² - 6x + 2y - 3 = 0 で同じ式。）

（2）P(5, 2) は 2² + 3² = 13 で確かに円上。中心 C(3, -1) に対し CP = (2, 3)。
　接線は P を通り CP に垂直：(p₀ - c)·(p - c) = r² → 2(x - 3) + 3(y + 1) = 13 → 2x + 3y - 16 = 0。
　（P を代入すると 10 + 6 - 16 = 0 で確認。）

（3）p·p - 2p·c = 0 → |p|² - 2p·c + |c|² = |c|² → |p - c|² = |c|²。
　中心 c = (1, 3)、半径 |c| の円（原点を通る）。

（4）半径 = |c| = √(1 + 9) = √10。
　【ポイント】p·(p - 2c) = 0 は「O と 2c を直径の両端とする円」と読める（(p - 0)·(p - 2c) = 0）。中心は O と 2c の中点 c、半径 |c|。
""")

# ---------------------------------------------------------------------
# mv_7 空間ベクトル
# ---------------------------------------------------------------------
P('q_mv_7_plus_basic', 'mv_7', '空間ベクトルの成分・内積・垂直', """
a = (1, 2, 2), b = (2, -2, 1) とする。

（1）|a| を求めよ。
（2）a と b のなす角 θ を求めよ。
（3）a, b の両方に垂直で、大きさが 3、x 成分が正のベクトル c を求めよ。
（4）点 P(1, 2, 2), Q(2, -2, 1) の距離 PQ を求めよ。
""", [
 dict(id='q_mv_7_plus_basic_1', label='（1）|a|', answer='3', accepted=[],
      check=lambda: norm(V(1,2,2)) == 3),
 dict(id='q_mv_7_plus_basic_2', label='（2）θ', answer='90°', accepted=['90','90度','π/2','直角'],
      check=lambda: dot(V(1,2,2),V(2,-2,1)) == 0),
 dict(id='q_mv_7_plus_basic_3', label='（3）c', answer='(2, 1, -2)', accepted=['(2,1,-2)','（2, 1, -2）'],
      check=lambda: dot(V(2,1,-2),V(1,2,2)) == 0 and dot(V(2,1,-2),V(2,-2,1)) == 0 and norm(V(2,1,-2)) == 3),
 dict(id='q_mv_7_plus_basic_4', label='（4）PQ', answer='3√2', accepted=['3√(2)','√18','3ルート2'],
      check=lambda: norm(V(2,-2,1)-V(1,2,2)) == 3*sqrt(2)),
], """
（1）|a| = √(1 + 4 + 4) = 3。

（2）a·b = 2 - 4 + 2 = 0 → 垂直、θ = 90°。（内積 0 は即「垂直」。cosθ を計算する前に内積の値を見る。）

（3）c = (p, q, r) とおく。c·a = p + 2q + 2r = 0、c·b = 2p - 2q + r = 0。
　辺々加えて 3p + 3r = 0 → r = -p。第 1 式に代入：p + 2q - 2p = 0 → q = p/2。∴ c = (p/2)(2, 1, -2)。
　|(2, 1, -2)| = 3 なので、大きさ 3 で x 成分正 → c = (2, 1, -2)。
　【別解】外積 a × b = (2·1 - 2·(-2), 2·2 - 1·1, 1·(-2) - 2·2) = (6, 3, -6) = 3(2, 1, -2)。

（4）PQ = |b - a| = |(1, -4, -1)| = √(1 + 16 + 1) = √18 = 3√2。
　a ⊥ b で |a| = |b| = 3 なので、O, P, Q は直角二等辺三角形。PQ = 3√2 は斜辺として整合する。
""")

P('q_mv_7_plus_triangle', 'mv_7', '空間の三角形（内積・角・面積）', """
3 点 A(2, 1, 3), B(4, -1, 1), C(1, 2, 2) について、次を求めよ。

（1）|AB|
（2）AB·AC
（3）cos∠BAC
（4）三角形 ABC の面積 S
""", [
 dict(id='q_mv_7_plus_triangle_1', label='（1）|AB|', answer='2√3', accepted=['2√(3)','√12','2ルート3'],
      check=lambda: norm(V(2,-2,-2)) == 2*sqrt(3)),
 dict(id='q_mv_7_plus_triangle_2', label='（2）AB·AC', answer='-2', accepted=[],
      check=lambda: dot(V(2,-2,-2),V(-1,1,-1)) == -2),
 dict(id='q_mv_7_plus_triangle_3', label='（3）cos∠BAC', answer='-1/3', accepted=['-(1/3)'],
      check=lambda: simplify(dot(V(2,-2,-2),V(-1,1,-1))/(norm(V(2,-2,-2))*norm(V(-1,1,-1))) - R(-1,3)) == 0),
 dict(id='q_mv_7_plus_triangle_4', label='（4）S', answer='2√2', accepted=['2√(2)','√8','2ルート2'],
      check=lambda: area(V(2,-2,-2),V(-1,1,-1)) == 2*sqrt(2)),
], """
AB = (2, -2, -2)、AC = (-1, 1, -1)。

（1）|AB| = √(4 + 4 + 4) = √12 = 2√3。

（2）AB·AC = -2 - 2 + 2 = -2。

（3）|AC| = √3。cos∠BAC = -2/(2√3·√3) = -2/6 = -1/3。負なので ∠BAC は鈍角。

（4）S = (1/2)√(|AB|²|AC|² - (AB·AC)²) = (1/2)√(12·3 - 4) = (1/2)√32 = 2√2。
　（sin∠BAC = √(1 - 1/9) = 2√2/3、S = (1/2)·2√3·√3·(2√2/3) = 2√2 でも一致。）

【空間でも公式は同じ】内積・なす角・面積の公式は平面と全く同じ形。成分が 3 つになるだけ。ただし「x₁y₂ - x₂y₁」の面積公式は平面専用なので、空間では √ の公式を使う。
""")

# ---------------------------------------------------------------------
# mv_8 共面条件・球面
# ---------------------------------------------------------------------
P('q_mv_8_plus_coplanar', 'mv_8', '共面条件 s + t + u = 1', """
四面体 OABC において OA = a, OB = b, OC = c とする。

（1）OP = (1/2)a + (1/3)b + k c を満たす点 P が平面 ABC 上にあるとき、k の値を求めよ。
（2）OQ = k(a + 2b + 5c) を満たす点 Q が平面 ABC 上にあるとき、OQ を a, b, c で表せ。
（3）辺 OA の中点を M、三角形 ABC の重心を G とする。直線 MG と平面 OBC の交点 R について、OR を b, c で表せ。
（4）（3）のとき、MG:GR を求めよ。
""", [
 dict(id='q_mv_8_plus_coplanar_1', label='（1）k', answer='1/6', accepted=['k = 1/6','k=1/6'],
      check=lambda: solve(R(1,2)+R(1,3)+k-1, k) == [R(1,6)]),
 dict(id='q_mv_8_plus_coplanar_2', label='（2）OQ', answer='(a + 2b + 5c)/8', accepted=['(a+2b+5c)/8','a/8 + b/4 + 5c/8','(1/8)a + (1/4)b + (5/8)c','(1/8)a + (2/8)b + (5/8)c','1/8a+1/4b+5/8c'],
      check=lambda: solve(k*(1+2+5)-1, k) == [R(1,8)]),
 dict(id='q_mv_8_plus_coplanar_3', label='（3）OR', answer='b + c', accepted=['b+c','c + b','c+b'],
      check=lambda: simplify((1-3)*a/2 + 3*(a+b+c)/3 - (b+c)) == 0),
 dict(id='q_mv_8_plus_coplanar_4', label='（4）MG:GR', answer='1:2', accepted=['1 : 2','1：2'],
      check=lambda: solve((1-t)/2 + t/3, t) == [3]),
], """
【共面条件】点 P が平面 ABC 上 ⇔ OP = s a + t b + u c で s + t + u = 1（O が平面 ABC 上にないとき）。

（1）1/2 + 1/3 + k = 1 → k = 1 - 5/6 = 1/6。

（2）k(1 + 2 + 5) = 1 → k = 1/8。OQ = (a + 2b + 5c)/8。

（3）OM = a/2、OG = (a + b + c)/3。R は直線 MG 上：OR = (1 - t)OM + t·OG = {(1 - t)/2 + t/3}a + (t/3)b + (t/3)c。
　R が平面 OBC 上 ⇔ a の係数が 0：(1 - t)/2 + t/3 = 0 → 3(1 - t) + 2t = 0 → t = 3。
　OR = (3/3)b + (3/3)c = b + c。

（4）t = 3 は「M から G の方向に MG の 3 倍進んだ位置」。MR = 3MG なので GR = 2MG、MG:GR = 1:2。

【ポイント】「平面 OBC 上 ⇔ a の係数 0」「平面 ABC 上 ⇔ 係数の和 1」。どちらの平面かで使う条件が変わる。
""")

P('q_mv_8_plus_sphere', 'mv_8', '球面の方程式', """
（1）2 点 A(1, 2, 3), B(3, -2, 5) を直径の両端とする球面の方程式を求めよ。
（2）球面 x^2 + y^2 + z^2 - 2x + 4y - 6z + 5 = 0 の中心の座標を求めよ。
（3）（2）の球面の半径を求めよ。
（4）（2）の球面と平面 z = 5 が交わってできる円の半径を求めよ。
""", [
 dict(id='q_mv_8_plus_sphere_1', label='（1）', answer='(x - 2)^2 + y^2 + (z - 4)^2 = 6', accepted=['(x-2)^2+y^2+(z-4)^2=6','x^2 + y^2 + z^2 - 4x - 8z + 14 = 0','x^2+y^2+z^2-4x-8z+14=0'],
      check=lambda: (V(1,2,3)+V(3,-2,5))/2 == V(2,0,4) and (norm(V(3,-2,5)-V(1,2,3))/2)**2 == 6 and expand((x-2)**2+symbols('y')**2+(symbols('z')-4)**2-6 - (x**2+symbols('y')**2+symbols('z')**2-4*x-8*symbols('z')+14)) == 0),
 dict(id='q_mv_8_plus_sphere_2', label='（2）中心', answer='(1, -2, 3)', accepted=['(1,-2,3)','（1, -2, 3）'],
      check=lambda: expand((x-1)**2+(symbols('y')+2)**2+(symbols('z')-3)**2-9 - (x**2+symbols('y')**2+symbols('z')**2-2*x+4*symbols('y')-6*symbols('z')+5)) == 0),
 dict(id='q_mv_8_plus_sphere_3', label='（3）半径', answer='3', accepted=[],
      check=lambda: 1+4+9-5 == 9),
 dict(id='q_mv_8_plus_sphere_4', label='（4）円の半径', answer='√5', accepted=['√(5)','ルート5'],
      check=lambda: sqrt(9-(5-3)**2) == sqrt(5)),
], """
（1）中心は AB の中点 (2, 0, 4)。半径は |AB|/2 = √(4 + 16 + 4)/2 = √24/2 = √6。
　∴ (x - 2)² + y² + (z - 4)² = 6。（(p - a)·(p - b) = 0 を展開しても同じ。）

（2）平方完成：(x - 1)² + (y + 2)² + (z - 3)² = 1 + 4 + 9 - 5 = 9。中心 (1, -2, 3)。

（3）半径 = √9 = 3。

（4）中心 (1, -2, 3) と平面 z = 5 の距離は |5 - 3| = 2。
　切り口の円の半径 r は r² = 3² - 2² = 5 → r = √5。
　【定番】「球と平面の交わり」は、中心から平面への距離 d を出して r = √(R² - d²)。円の中心は (1, -2, 5)。
""")
