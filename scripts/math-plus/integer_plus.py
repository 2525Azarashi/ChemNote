# -*- coding: utf-8 -*-
"""整数 入試レベル強化問題（Python 全列挙・sympy で検算）"""
from harness import *
from sympy import Rational as R, divisors, divisor_count, divisor_sigma, gcd, lcm, factorint, isprime, Mod
from itertools import product
from fractions import Fraction as Fr

# ---------------------------------------------------------------------
# mi_1 約数・倍数
# ---------------------------------------------------------------------
P('q_mi_1_plus_div', 'mi_1', '約数の個数・総和・素因数分解の活用', """
（1）360 の正の約数の個数を求めよ。
（2）360 の正の約数の総和を求めよ。
（3）正の約数がちょうど 6 個である最小の自然数を求めよ。
（4）√(540n) が自然数となる最小の自然数 n を求めよ。
""", [
 dict(id='q_mi_1_plus_div_1', label='（1）', answer='24', accepted=['24個','24 個'], check=lambda: divisor_count(360) == 24),
 dict(id='q_mi_1_plus_div_2', label='（2）', answer='1170', accepted=[], check=lambda: divisor_sigma(360) == 1170),
 dict(id='q_mi_1_plus_div_3', label='（3）', answer='12', accepted=[], check=lambda: min(n_ for n_ in range(1,200) if divisor_count(n_)==6) == 12),
 dict(id='q_mi_1_plus_div_4', label='（4）', answer='15', accepted=['n = 15','n=15'], check=lambda: min(n_ for n_ in range(1,200) if int((540*n_)**0.5+0.5)**2==540*n_) == 15),
], """
（1）360 = 2³·3²·5。約数の個数は (3+1)(2+1)(1+1) = 24 個。

（2）総和 = (1 + 2 + 4 + 8)(1 + 3 + 9)(1 + 5) = 15·13·6 = 1170。

（3）約数 6 個 ⇔ (指数+1) の積が 6 ⇔ 指数の組が (5) または (2, 1)。
　p⁵ 型の最小は 2⁵ = 32。p²q 型の最小は 2²·3 = 12。∴ 12。
　「約数の個数から素因数分解の形を逆算する」典型。

（4）540 = 2²·3³·5。すべての指数を偶数にするには 3 と 5 を 1 つずつ補う：n = 3·5 = 15。
　√(540·15) = √8100 = 90。
""")

def _gcd_lcm_pairs(g, l):
    return sorted((a_, l*g//a_) for a_ in range(1, l+1) if a_ <= l*g//a_ and l*g % a_ == 0 and gcd(a_, l*g//a_) == g and lcm(a_, l*g//a_) == l)

P('q_mi_1_plus_gcd', 'mi_1', '最大公約数・最小公倍数の条件から数を決める', """
（1）2 つの自然数 a, b（a < b）の最大公約数が 6、最小公倍数が 180 であるとき、組 (a, b) はいくつあるか。
（2）（1）のうち、a + b が最小となる組の a + b の値を求めよ。
（3）n と 36 の最大公約数が 12 で、n < 100 となる自然数 n をすべて求めよ。
（4）1 から 100 までの自然数のうち、100 と互いに素なものの個数を求めよ。
""", [
 dict(id='q_mi_1_plus_gcd_1', label='（1）', answer='4', accepted=['4組','4 組','4個'], check=lambda: len(_gcd_lcm_pairs(6,180)) == 4),
 dict(id='q_mi_1_plus_gcd_2', label='（2）', answer='66', accepted=[], check=lambda: min(a_+b_ for a_,b_ in _gcd_lcm_pairs(6,180)) == 66),
 dict(id='q_mi_1_plus_gcd_3', label='（3）', answer='12, 24, 48, 60, 84, 96', accepted=['12,24,48,60,84,96','n = 12, 24, 48, 60, 84, 96'],
      check=lambda: [n_ for n_ in range(1,100) if gcd(n_,36)==12] == [12,24,48,60,84,96]),
 dict(id='q_mi_1_plus_gcd_4', label='（4）', answer='40', accepted=['40個','40 個'], check=lambda: sum(1 for n_ in range(1,101) if gcd(n_,100)==1) == 40),
], """
（1）a = 6m、b = 6n（m < n、m と n は互いに素）とおくと、最小公倍数は 6mn = 180 → mn = 30。
　互いに素な (m, n)：(1, 30)(2, 15)(3, 10)(5, 6) の 4 組。（(1,30) は互いに素。(2,15)(3,10)(5,6) も互いに素。）∴ 4 組。
　【注意】mn = 30 の分解 (1,30)(2,15)(3,10)(5,6) はすべて互いに素なので落ちるものはないが、例えば mn = 12 なら (2, 6) は互いに素でなく除外される。

（2）(a, b) = (6, 180)(12, 90)(18, 60)(30, 36)。和が最小なのは (30, 36) で 66。

（3）gcd(n, 36) = 12 ⇔ n = 12k で gcd(k, 3) = 1（36 = 12·3）。k = 1, 2, 4, 5, 7, 8（k < 100/12 ≈ 8.3 で 3 の倍数を除く）。
　n = 12, 24, 48, 60, 84, 96。

（4）100 = 2²·5²。2 の倍数 50 個、5 の倍数 20 個、10 の倍数 10 個。100 - (50 + 20 - 10) = 40 個。
　【オイラー関数】φ(100) = 100(1 - 1/2)(1 - 1/5) = 40 で一致。
""")

# ---------------------------------------------------------------------
# mi_2 互除法・1次不定方程式
# ---------------------------------------------------------------------
P('q_mi_2_plus_euclid', 'mi_2', 'ユークリッドの互除法と 1 次不定方程式', """
（1）ユークリッドの互除法を用いて 1071 と 1029 の最大公約数を求めよ。
（2）方程式 17x + 5y = 1 の整数解を 1 つ求めよ（x が 0 以上で最小のもの）。
（3）方程式 17x + 5y = 1 のすべての整数解を x = 5k + □ の形で表すとき、□ に入る 0 以上 5 未満の整数を求めよ。
（4）方程式 17x + 5y = 100 を満たす自然数の組 (x, y) をすべて求めよ。
""", [
 dict(id='q_mi_2_plus_euclid_1', label='（1）', answer='21', accepted=[], check=lambda: gcd(1071,1029) == 21),
 dict(id='q_mi_2_plus_euclid_2', label='（2）', answer='(3, -10)', accepted=['(3,-10)','x = 3, y = -10','x=3,y=-10','（3, -10）'],
      check=lambda: 17*3+5*(-10) == 1 and all(17*x_+5*y_ != 1 for x_ in range(0,3) for y_ in range(-40,40))),
 dict(id='q_mi_2_plus_euclid_3', label='（3）□', answer='3', accepted=[], check=lambda: 3 % 5 == 3),
 dict(id='q_mi_2_plus_euclid_4', label='（4）(x, y)', answer='(5, 3)', accepted=['(5,3)','x = 5, y = 3','x=5,y=3','（5, 3）'],
      check=lambda: [(x_,y_) for x_ in range(1,100) for y_ in range(1,100) if 17*x_+5*y_==100] == [(5,3)]),
], """
（1）1071 = 1029·1 + 42、1029 = 42·24 + 21、42 = 21·2 + 0。∴ gcd = 21。
　「大きい方を小さい方で割り、余りが 0 になる直前の余り」が最大公約数。

（2）互除法を逆にたどる：17 = 5·3 + 2、5 = 2·2 + 1。
　1 = 5 - 2·2 = 5 - (17 - 5·3)·2 = 5·7 - 17·2。∴ 17·(-2) + 5·7 = 1。
　x ≧ 0 で最小にするには x に 5 を足す（一般解 x = -2 + 5k）：k = 1 で (x, y) = (3, -10)。検算：51 - 50 = 1。
　（小さい係数なら「17x を 5 で割った余りが 1 になる x を探す」：17·3 = 51 ≡ 1 (mod 5) で直接見つかる。）

（3）特殊解 (3, -10) から 17(x - 3) = -5(y + 10)。17 と 5 は互いに素なので x - 3 = 5k、y + 10 = -17k。
　x = 5k + 3、y = -17k - 10。□ = 3。

（4）17x + 5y = 100。(2) の解を 100 倍：特殊解 (300, -1000)。一般解 x = 300 + 5k、y = -1000 - 17k。
　x ≧ 1 かつ y ≧ 1：300 + 5k ≧ 1 → k ≧ -59.8、-1000 - 17k ≧ 1 → k ≦ -58.9。∴ k = -59 のみ → (x, y) = (5, 3)。
　【近道】5y = 100 - 17x は 5 の倍数なので 17x ≡ 0 (mod 5) → x は 5 の倍数。x = 5 → y = 3。x = 10 → y < 0。
""")

P('q_mi_2_plus_dioph', 'mi_2', '1 次不定方程式の応用（文章題・余りの連立）', """
（1）7 で割ると 3 余り、5 で割ると 2 余る自然数のうち、最小のものを求めよ。
（2）（1）の条件を満たす自然数を小さい方から並べたとき、100 以下のものは何個あるか。
（3）1 個 120 円のりんごと 1 個 90 円のみかんを合わせて 1500 円分（おつりなし）買う。りんご・みかんをそれぞれ 1 個以上買うとき、買い方は何通りあるか。
（4）（3）のうち、個数の合計が最も多くなる買い方でのりんごの個数を求めよ。
""", [
 dict(id='q_mi_2_plus_dioph_1', label='（1）', answer='17', accepted=[], check=lambda: min(n_ for n_ in range(1,200) if n_%7==3 and n_%5==2) == 17),
 dict(id='q_mi_2_plus_dioph_2', label='（2）', answer='3', accepted=['3個','3 個'], check=lambda: sum(1 for n_ in range(1,101) if n_%7==3 and n_%5==2) == 3),
 dict(id='q_mi_2_plus_dioph_3', label='（3）', answer='4', accepted=['4通り','4 通り'], check=lambda: len([(a_,b_) for a_ in range(1,20) for b_ in range(1,20) if 120*a_+90*b_==1500]) == 4),
 dict(id='q_mi_2_plus_dioph_4', label='（4）', answer='2', accepted=['2個','2 個'],
      check=lambda: max([(a_,b_) for a_ in range(1,20) for b_ in range(1,20) if 120*a_+90*b_==1500], key=lambda p_: p_[0]+p_[1])[0] == 2),
], """
（1）n = 7a + 3 = 5b + 2 → 7a - 5b = -1。a = 2, b = 3 が解（14 - 15 = -1）。n = 17。
　「7 で割って 3 余る数」3, 10, 17, 24, … を書き出し、5 で割って 2 余るものを探しても速い（17 が最初）。

（2）一般解は n = 17 + 35k（7 と 5 の最小公倍数 35 ごと）。17, 52, 87 の 3 個（122 は超過）。

（3）120x + 90y = 1500 → 両辺を 30 で割って 4x + 3y = 50。
　x ≡ 2 (mod 3)（4x ≡ 50 ≡ 2 (mod 3)、4 ≡ 1 なので x ≡ 2）：x = 2, 5, 8, 11 → y = 14, 10, 6, 2。x = 14 だと y < 0。∴ 4 通り。
　【最初に割る】係数の最大公約数 30 で割ると数字が小さくなり、mod の処理が楽になる。

（4）(2, 14)(5, 10)(8, 6)(11, 2) の個数合計は 16, 15, 14, 13。最も多いのは (2, 14) でりんご 2 個。
　安い方（みかん）を多く買うほど個数が増える、という直感とも一致。
""")

# ---------------------------------------------------------------------
# mi_3 因数分解の利用
# ---------------------------------------------------------------------
def _sols(f, rng=range(-60,61)):
    return sorted((x_,y_) for x_ in rng for y_ in rng if f(x_,y_))

P('q_mi_3_plus_factor', 'mi_3', '積の形に直して約数の組合せ', """
（1）xy - 2x - 3y = 0 を満たす自然数の組 (x, y) をすべて求めよ。
（2）x^2 - y^2 = 45 を満たす自然数の組 (x, y) は何組あるか。
（3）1/x + 1/y = 1/4 を満たす自然数の組 (x, y)（x ≦ y）をすべて求めよ。
（4）n^2 + 2n - 8 が素数となる自然数 n を求めよ。
""", [
 dict(id='q_mi_3_plus_factor_1', label='（1）', answer='(4, 8), (5, 5), (6, 4), (9, 3)',
      accepted=['(4,8),(5,5),(6,4),(9,3)','(4, 8)(5, 5)(6, 4)(9, 3)','(x, y) = (4, 8), (5, 5), (6, 4), (9, 3)','(4,8), (5,5), (6,4), (9,3)'],
      check=lambda: _sols(lambda x_,y_: x_>0 and y_>0 and x_*y_-2*x_-3*y_==0, range(1,100)) == [(4,8),(5,5),(6,4),(9,3)]),
 dict(id='q_mi_3_plus_factor_2', label='（2）', answer='3', accepted=['3組','3 組','3個'],
      check=lambda: len(_sols(lambda x_,y_: x_>0 and y_>0 and x_*x_-y_*y_==45, range(1,100))) == 3),
 dict(id='q_mi_3_plus_factor_3', label='（3）', answer='(5, 20), (6, 12), (8, 8)',
      accepted=['(5,20),(6,12),(8,8)','(5, 20)(6, 12)(8, 8)','(x, y) = (5, 20), (6, 12), (8, 8)','(5,20), (6,12), (8,8)'],
      check=lambda: _sols(lambda x_,y_: x_>0 and y_>0 and x_<=y_ and 4*(x_+y_)==x_*y_, range(1,100)) == [(5,20),(6,12),(8,8)]),
 dict(id='q_mi_3_plus_factor_4', label='（4）n', answer='3', accepted=['n = 3','n=3'],
      check=lambda: [n_ for n_ in range(1,300) if isprime(n_*n_+2*n_-8)] == [3]),
], """
（1）xy - 2x - 3y = 0 → (x - 3)(y - 2) = 6。（x·y - 2x - 3y + 6 = 6 と定数を補う。）
　x - 3 ≧ -2、y - 2 ≧ -1 で積が正の 6 → 両方正：(x-3, y-2) = (1,6)(2,3)(3,2)(6,1)。
　(x, y) = (4, 8)(5, 5)(6, 4)(9, 3)。
　【補う定数】xy + ax + by = (x + b)(y + a) - ab。この形を暗記。

（2）(x + y)(x - y) = 45、x + y > x - y > 0 かつ偶奇一致（45 は奇数なので両方奇数）。
　(x+y, x-y) = (45, 1)(15, 3)(9, 5) → (x, y) = (23, 22)(9, 6)(7, 2)。3 組。

（3）両辺に 4xy をかけて 4y + 4x = xy → (x - 4)(y - 4) = 16。x ≦ y なので x - 4 ≦ y - 4。
　x - 4 > 0（x - 4 < 0 だと y - 4 < 0 で y < 4 となり 1/x + 1/y > 1/4 + 1/4 で矛盾）。
　(x-4, y-4) = (1, 16)(2, 8)(4, 4) → (x, y) = (5, 20)(6, 12)(8, 8)。

（4）n² + 2n - 8 = (n + 4)(n - 2)。素数 p は「1 × p」としか積に分解できないので、小さい方の因数 n - 2 が 1：n = 3。
　このとき値は 7·1 = 7 で確かに素数。n ≧ 4 では n - 2 ≧ 2、n + 4 ≧ 8 で合成数。n = 1, 2 では値が負または 0。
　【素数条件の型】「因数分解 → 一方の因数が ±1」。負の因数の可能性（(-1)×(-p)）も本来は確認する：n + 4 = -1 は自然数では不可能。
""")

P('q_mi_3_plus_prime', 'mi_3', '2 次式の因数分解と素数・平方数の条件', """
（1）x^2 + xy - 2y^2 = 7 を満たす自然数の組 (x, y) を求めよ。
（2）xy + x + y = 23 を満たす自然数の組 (x, y) は何組あるか。
（3）p, p + 2, p + 4 がすべて素数となる素数 p を求めよ。
（4）n^2 + 3n + 3 が n + 1 で割り切れる自然数 n の個数を求めよ。
""", [
 dict(id='q_mi_3_plus_prime_1', label='（1）', answer='(3, 2)', accepted=['(3,2)','x = 3, y = 2','x=3,y=2','（3, 2）'],
      check=lambda: _sols(lambda x_,y_: x_>0 and y_>0 and x_*x_+x_*y_-2*y_*y_==7, range(1,200)) == [(3,2)]),
 dict(id='q_mi_3_plus_prime_2', label='（2）', answer='6', accepted=['6組','6 組','6個'],
      check=lambda: len(_sols(lambda x_,y_: x_>0 and y_>0 and x_*y_+x_+y_==23, range(1,100))) == 6),
 dict(id='q_mi_3_plus_prime_3', label='（3）p', answer='3', accepted=['p = 3','p=3'],
      check=lambda: [p_ for p_ in range(2,2000) if isprime(p_) and isprime(p_+2) and isprime(p_+4)] == [3]),
 dict(id='q_mi_3_plus_prime_4', label='（4）', answer='0', accepted=['0個','0 個','存在しない','ない'],
      check=lambda: [n_ for n_ in range(1,2000) if (n_*n_+3*n_+3)%(n_+1)==0] == []),
], """
（1）左辺を因数分解：x² + xy - 2y² = (x + 2y)(x - y)。x + 2y > 0 なので x - y > 0、かつ x + 2y > x - y。
　7 は素数なので (x + 2y, x - y) = (7, 1)。連立して 3y = 6 → y = 2、x = 3。∴ (3, 2)。

（2）xy + x + y + 1 = 24 → (x + 1)(y + 1) = 24。x + 1 ≧ 2、y + 1 ≧ 2 の約数の組：(2,12)(3,8)(4,6)(6,4)(8,3)(12,2) の 6 組。
　（(1, 24)(24, 1) は x または y が 0 になるので除外。）

（3）連続する 3 つの奇数 p, p+2, p+4 のうち、どれか 1 つは必ず 3 の倍数（3 で割った余りが 0, 2, 1 と一巡する）。
　3 の倍数の素数は 3 のみ → p = 3（3, 5, 7）。p + 2 = 3 なら p = 1 で素数でない、p + 4 = 3 は負。∴ p = 3。
　【余りで分類】「連続 k 個のうち 1 つは k の倍数」は素数問題の定番の武器。

（4）n² + 3n + 3 = (n + 1)(n + 2) + 1。n + 1 で割ると余り 1。よって n + 1 が 1 を割り切る必要があり n + 1 = 1 → n = 0（自然数でない）。
　∴ 該当する自然数は存在しない：0 個。
　【割り算の形にする】「f(n) が n + 1 で割り切れる」は f(n) を n + 1 で割った余り（定数）が n + 1 の倍数、と読み替える。
""")

# ---------------------------------------------------------------------
# mi_4 余りによる分類
# ---------------------------------------------------------------------
P('q_mi_4_plus_mod', 'mi_4', '余りの計算（合同式・累乗）', """
（1）3^100 を 7 で割った余りを求めよ。
（2）2^50 を 9 で割った余りを求めよ。
（3）7^2024 の一の位の数を求めよ。
（4）1^2 + 2^2 + 3^2 + … + 100^2 を 4 で割った余りを求めよ。
""", [
 dict(id='q_mi_4_plus_mod_1', label='（1）', answer='4', accepted=[], check=lambda: pow(3,100,7) == 4),
 dict(id='q_mi_4_plus_mod_2', label='（2）', answer='4', accepted=[], check=lambda: pow(2,50,9) == 4),
 dict(id='q_mi_4_plus_mod_3', label='（3）', answer='1', accepted=[], check=lambda: pow(7,2024,10) == 1),
 dict(id='q_mi_4_plus_mod_4', label='（4）', answer='2', accepted=[], check=lambda: sum(i_*i_ for i_ in range(1,101)) % 4 == 2),
], """
合同式：a ≡ b (mod m) なら a^k ≡ b^k。「1 または -1 になる累乗」を探すのが最速。

（1）3³ = 27 ≡ -1 (mod 7)。3^100 = 3^99·3 = (3³)^33·3 ≡ (-1)^33·3 = -3 ≡ 4。∴ 余り 4。
　（3⁶ ≡ 1 を使うなら 3^100 = 3^96·3⁴ ≡ 3⁴ = 81 ≡ 4。）

（2）2³ = 8 ≡ -1 (mod 9)。2^50 = 2^48·2² = (2³)^16·4 ≡ 1·4 = 4。∴ 余り 4。

（3）一の位 ⇔ mod 10。7² = 49 ≡ -1 (mod 10)。7^2024 = (7²)^1012 ≡ (-1)^1012 = 1。∴ 一の位は 1。
　（7 の累乗の一の位は 7, 9, 3, 1 の周期 4。2024 は 4 の倍数なので 1。）

（4）平方数を 4 で割った余りは、偶数² ≡ 0、奇数² ≡ 1。1〜100 に奇数は 50 個 → 和 ≡ 50 ≡ 2 (mod 4)。
　【公式で検算】Σk² = 100·101·201/6 = 338350。338350 ÷ 4 = 84587 余り 2。
""")

def _all_n(f, rng=range(-50,51)): return all(f(n_) for n_ in rng)

P('q_mi_4_plus_class', 'mi_4', '余りで分類する証明・平方数の余り', """
（1）平方数を 3 で割った余りとして現れる数をすべて求めよ。
（2）平方数を 8 で割った余りとして現れる数をすべて求めよ。
（3）n が奇数のとき、n^2 - 1 は必ず □ の倍数である。□ に入る最大の自然数を求めよ。
（4）x^2 + y^2 = 2023 を満たす整数 x, y は存在するか。「存在する」「存在しない」で答えよ。
""", [
 dict(id='q_mi_4_plus_class_1', label='（1）', answer='0, 1', accepted=['0,1','0と1','0 と 1','1, 0'],
      check=lambda: sorted({(n_*n_)%3 for n_ in range(100)}) == [0,1]),
 dict(id='q_mi_4_plus_class_2', label='（2）', answer='0, 1, 4', accepted=['0,1,4','0と1と4','0 と 1 と 4'],
      check=lambda: sorted({(n_*n_)%8 for n_ in range(100)}) == [0,1,4]),
 dict(id='q_mi_4_plus_class_3', label='（3）□', answer='8', accepted=['8の倍数'],
      check=lambda: __import__('math').gcd(*[n_*n_-1 for n_ in range(3,60,2)]) == 8),
 dict(id='q_mi_4_plus_class_4', label='（4）', answer='存在しない', accepted=['存在しない。','ない','しない'],
      check=lambda: 2023 % 4 == 3 and not any(x_*x_+y_*y_==2023 for x_ in range(0,50) for y_ in range(0,50))),
], """
（1）n = 3k, 3k±1 で n² = 9k², 9k² ± 6k + 1 → 余り 0 または 1。「2 は現れない」。

（2）奇数 n = 2k + 1 で n² = 4k(k+1) + 1。k(k+1) は偶数なので n² ≡ 1 (mod 8)。
　偶数 n = 2k で n² = 4k²：k 偶数なら ≡ 0、k 奇数なら ≡ 4。∴ 0, 1, 4。

（3）n² - 1 = (n - 1)(n + 1)。n が奇数なら n - 1, n + 1 は連続する 2 つの偶数で、一方は 4 の倍数。積は 8 の倍数。
　n = 3 で 8、n = 5 で 24：最大公約数は 8 なので「必ず」言えるのは 8 の倍数まで。（(2) の「奇数² ≡ 1 (mod 8)」と同じ内容。）

（4）平方数を 4 で割った余りは 0 か 1。x² + y² を 4 で割った余りは 0, 1, 2 のいずれかで、3 にはならない。
　2023 = 4·505 + 3 → 余り 3。∴ 存在しない。
　【定番】「x² + y² = N」は mod 4（または mod 3, mod 8）で不可能性を示す。
""")

# ---------------------------------------------------------------------
# mi_5 範囲の絞り込み・n進法
# ---------------------------------------------------------------------
P('q_mi_5_plus_bound', 'mi_5', '不等式で範囲を絞る', """
（1）1/x + 1/y + 1/z = 1 を満たす自然数の組 (x, y, z)（x ≦ y ≦ z）は何組あるか。
（2）1/x + 1/y = 1/3 を満たす自然数の組 (x, y)（x ≦ y）をすべて求めよ。
（3）x + y + z = xyz を満たす自然数の組 (x, y, z)（x ≦ y ≦ z）を求めよ。
（4）自然数 n について、n^2 < 2^n が成り立つ最小の n（n ≧ 2）を求めよ。
""", [
 dict(id='q_mi_5_plus_bound_1', label='（1）', answer='3', accepted=['3組','3 組','3個'],
      check=lambda: sorted((x_,y_,z_) for x_ in range(1,40) for y_ in range(x_,40) for z_ in range(y_,40) if Fr(1,x_)+Fr(1,y_)+Fr(1,z_)==1) == [(2,3,6),(2,4,4),(3,3,3)]),
 dict(id='q_mi_5_plus_bound_2', label='（2）', answer='(4, 12), (6, 6)',
      accepted=['(4,12),(6,6)','(4, 12)(6, 6)','(x, y) = (4, 12), (6, 6)'],
      check=lambda: sorted((x_,y_) for x_ in range(1,100) for y_ in range(x_,100) if Fr(1,x_)+Fr(1,y_)==Fr(1,3)) == [(4,12),(6,6)]),
 dict(id='q_mi_5_plus_bound_3', label='（3）', answer='(1, 2, 3)', accepted=['(1,2,3)','x = 1, y = 2, z = 3','（1, 2, 3）'],
      check=lambda: sorted((x_,y_,z_) for x_ in range(1,30) for y_ in range(x_,30) for z_ in range(y_,30) if x_+y_+z_==x_*y_*z_) == [(1,2,3)]),
 dict(id='q_mi_5_plus_bound_4', label='（4）', answer='5', accepted=['n = 5','n=5'],
      check=lambda: min(n_ for n_ in range(2,100) if n_*n_ < 2**n_) == 5),
], """
【範囲の絞り込み】対称式では x ≦ y ≦ z と大小を仮定し、最小の文字 x について「1/x が最も大きい」ことから x の範囲を出す。

（1）1 = 1/x + 1/y + 1/z ≦ 3/x → x ≦ 3。また 1/x < 1 → x ≧ 2。
　x = 2：1/y + 1/z = 1/2、1/2 ≦ 2/y → y ≦ 4、1/y < 1/2 → y ≧ 3。y = 3 → z = 6、y = 4 → z = 4。
　x = 3：1/y + 1/z = 2/3、2/3 ≦ 2/y → y ≦ 3、y ≧ 3 → y = 3, z = 3。
　∴ (2, 3, 6), (2, 4, 4), (3, 3, 3) の 3 組。
　【落とし穴】x = 2 のとき y = 3 を見落として (2, 4, 4) だけにしがち。y の範囲を不等式で両側から確定させる。

（2）1/3 = 1/x + 1/y ≦ 2/x → x ≦ 6。1/x < 1/3 → x ≧ 4。x = 4 → 1/y = 1/12、y = 12。x = 5 → 1/y = 2/15（不可）。x = 6 → y = 6。
　∴ (4, 12), (6, 6)。（(x - 3)(y - 3) = 9 の積の形でも同じ結果。）

（3）xyz = x + y + z ≦ 3z → xy ≦ 3。(x, y) = (1, 1)：2 + z = z で不可。(1, 2)：3 + z = 2z → z = 3。(1, 3)：4 + z = 3z → z = 2 だが y ≦ z に反する。
　∴ (1, 2, 3)。「最大の文字 z で押さえて xy ≦ 3 に絞る」のが急所。

（4）n = 2: 4 < 4 ✗、n = 3: 9 < 8 ✗、n = 4: 16 < 16 ✗、n = 5: 25 < 32 ✓。∴ n = 5。
　（n ≧ 5 で常に成り立つことは数学的帰納法で示せる：n² < 2^n → (n+1)² = n² + 2n + 1 < 2n² < 2^(n+1)、n ≧ 3 で 2n + 1 < n²。）
""")

P('q_mi_5_plus_base', 'mi_5', 'n 進法の相互変換と桁数', """
（1）2 進数 1011011(2) を 10 進数で表せ。
（2）10 進数 345 を 8 進数で表せ。
（3）3 進数 2102(3) を 10 進数で表せ。
（4）10 進数 200 を 2 進数で表したとき、桁数はいくつか。
（5）5 進数で表すと 3 桁になる自然数は何個あるか。
""", [
 dict(id='q_mi_5_plus_base_1', label='（1）', answer='91', accepted=[], check=lambda: int('1011011',2) == 91),
 dict(id='q_mi_5_plus_base_2', label='（2）', answer='531(8)', accepted=['531','531_(8)','531₍₈₎','(531)8'], check=lambda: oct(345)[2:] == '531'),
 dict(id='q_mi_5_plus_base_3', label='（3）', answer='65', accepted=[], check=lambda: int('2102',3) == 65),
 dict(id='q_mi_5_plus_base_4', label='（4）', answer='8', accepted=['8桁','8 桁'], check=lambda: len(format(200,'b')) == 8),
 dict(id='q_mi_5_plus_base_5', label='（5）', answer='100', accepted=['100個','100 個'], check=lambda: sum(1 for n_ in range(1,1000) if 25 <= n_ < 125) == 100),
], """
（1）右から 2⁰, 2¹, … の重み：1·64 + 0·32 + 1·16 + 1·8 + 0·4 + 1·2 + 1·1 = 64 + 16 + 8 + 2 + 1 = 91。

（2）8 で割り続けて余りを下から読む：345 ÷ 8 = 43 余り 1、43 ÷ 8 = 5 余り 3、5 ÷ 8 = 0 余り 5 → 531(8)。
　検算：5·64 + 3·8 + 1 = 320 + 24 + 1 = 345 ✓。

（3）2·27 + 1·9 + 0·3 + 2·1 = 54 + 9 + 2 = 65。

（4）2⁷ = 128 ≦ 200 < 256 = 2⁸ なので 2 進数で 8 桁（200 = 11001000(2)）。
　【桁数の判定】n 進数で k 桁 ⇔ n^(k-1) ≦ N < n^k。

（5）5 進数で 3 桁 ⇔ 5² ≦ N < 5³ ⇔ 25 ≦ N ≦ 124。個数は 124 - 25 + 1 = 100 個。
　（「100(5) から 444(5) まで」＝ 4·5·5 = 100 個、と各桁の選び方で数えても同じ。）
""")
