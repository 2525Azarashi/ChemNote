# -*- coding: utf-8 -*-
"""マナトビ基本演習 Step 87〜96（場合の数：数え方の法則・順列）"""
from comb_common import *
from math import comb, perm, factorial as fact
import itertools

# ============================================================
# ia5_1 数え方の法則（和の法則・積の法則・約数）
# ============================================================
_amts = {a*10 + b*100 + c*500 for a in range(5) for b in range(3) for c in range(3)} - {0}
PROB(87, 'ia5_1', '金額の種類',
 '財布に10円玉が4枚、100円玉が2枚、500円玉が2枚入っている。これらの一部または全部を使って支払うとき、支払える金額は何通りあるか。',
 [S(1, '答', None, 44,
    '10円玉は0〜4枚の5通り、100円玉は0〜2枚の3通り、500円玉は0〜2枚の3通り。10円玉4枚では100円に届かず、100円玉2枚でも500円に届かないので、枚数の組ごとに金額はすべて異なる。1枚も使わない0円を除いて 5×3×3−1＝44（通り）。',
    [('45通り（0円も数えた値）', '1枚も使わない 0 円は「支払える金額」ではないので除く。'),
     ('16通り（各硬貨を1枚以上使うとした値）', '「一部」を使うので 0 枚の硬貨があってよい。4×2×2 ではない。'),
     ('8通り（硬貨の種類数を足した値）', '枚数ごとの通り数を「かける」。4＋2＋2 ではない。')],
    unit='通り', check=lambda: len(_amts) == 44)],
 '同じ金額になる枚数の組（両替できる組）がないかを先に確かめる。無ければ枚数の積から 0 円の 1 通りを引く。')

_cards = [0, 0, 1, 1, 2, 2, 3, 3, 4, 4]
_n3 = {t for t in itertools.permutations(_cards, 3) if t[0] != 0}
PROB(88, 'ia5_1', '0 を使う・使わないで分けて数える',
 '0, 1, 2, 3, 4 の数字が書かれたカードが2枚ずつ、合計10枚ある。このうち3枚を並べて3桁の整数をつくる。同じ数字のカードは区別しない。',
 [S(1, '（1）', '0 のカードを使わない3桁の整数はいくつあるか。', 60,
    '1〜4 から作る。3 つとも異なる：₄P₃＝24。2 つが同じ：同じにする数字 4 通り×残りの数字 3 通り×並べ方 3 通り＝36。各数字は 2 枚しかないので 3 つ同じはできない。24＋36＝60（個）。',
    [('24個（3つとも異なる数字だけを数えた値）', '同じ数字が 2 枚ずつあるので「112」のように 2 つ同じ数字も作れる。'),
     ('64個（1〜4を何度でも使えるとした 4³）', '各数字は 2 枚しかないので「111」などは作れない。'),
     ('48個（2つ同じ数字の並べ方を2通りと数えた値）', '「112」「121」「211」のように同じ数字 2 つの並べ方は 3 通り。')],
    unit='個', check=lambda: sum(1 for t in _n3 if 0 not in t) == 60),
  S(2, '（2）', '0 のカードを使う3桁の整数はいくつあるか。', 36,
    '百の位は 0 以外。0 を 1 枚使う：0 の位置（十・一）2 通り×残り 2 つの位（1〜4、同じ数字も可）4×4＝16 で 32。0 を 2 枚使う：百の位の 4 通り（□00）。32＋4＝36（個）。',
    [('32個（0を2枚使う □00 を落とした値）', '0 のカードは 2 枚あるので 100, 200, 300, 400 も作れる。'),
     ('40個（□00 型を2倍に数えた値）', '□00 の 2 つの 0 は区別しないので百の位の 4 通りだけ。'),
     ('54個（0を百の位にも置いた値）', '0 は百の位（最高位）に置けない。')],
    unit='個', check=lambda: sum(1 for t in _n3 if 0 in t) == 36),
  S(3, '（3）', '3桁の整数は全部でいくつあるか。', 96,
    '0 を使う場合と使わない場合は同時に起こらないので、和の法則により 60＋36＝96（個）。',
    [('100個（□00 型を重ねて数えた値）', '0 を使う・使わないで分けたので重なりは無い。60＋36 のまま足す。'),
     ('120個（0も百の位に置けるとした値）', '最高位に 0 は置けない。'),
     ('92個（0を2枚使う場合を入れ忘れた値）', '0 を 2 枚使う □00 型の 4 個も足す。')],
    unit='個', check=lambda: len(_n3) == 96)],
 '最高位に 0 を置けないので「0 を使う／使わない」に分け、和の法則で足す。')

PROB(89, 'ia5_1', '各位の通り数をかける',
 '0, 1, 2, 3, 4 の5種類の数字を使って4桁の整数をつくる。',
 [S(1, '（1）', '同じ数字を何度使ってもよいとき、4桁の整数は何個できるか。', 500,
    '千の位は 1〜4 の 4 通り、百・十・一の位は 0〜4 の 5 通りずつ。積の法則より 4×5×5×5＝500（個）。',
    [('625個（千の位にも0を置いた 5⁴）', '千の位に 0 を置くと 4 桁にならない。'),
     ('256個（すべての位を1〜4とした 4⁴）', '千の位以外は 0 を置いてよい。'),
     ('96個（同じ数字を使わないとして数えた値）', '同じ数字を何度使ってもよい条件を落としている。')],
    unit='個', check=lambda: sum(1 for t in itertools.product(range(5), repeat=4) if t[0]) == 500),
  S(2, '（2）', '同じ数字を2度以上使わないとき、4桁の整数は何個できるか。', 96,
    '千の位は 0 以外の 4 通り、百の位は千の位以外の 4 通り、十の位は 3 通り、一の位は 2 通り。4×4×3×2＝96（個）。',
    [('120個（₅P₄ をそのまま答えた値）', '₅P₄ には 0 が千の位に来る 24 個が含まれる。'),
     ('72個（百の位を3通りとした値）', '百の位は 0 も使えるので、千の位で使った数字以外の 4 通り。'),
     ('24個（0を使わない 4! だけ数えた値）', '百・十・一の位には 0 を置いてよい。')],
    unit='個', check=lambda: sum(1 for t in itertools.permutations(range(5), 4) if t[0]) == 96)],
 '最高位に 0 を置けないことを最初に処理し、各位の通り数をかける。')

_d180 = [k for k in range(1, 181) if 180 % k == 0]
PROB(90, 'ia5_1', '約数の個数と総和',
 '180 の正の約数について答えよ。',
 [S(1, '（1）', '正の約数の個数を求めよ。', 18,
    '180＝2²×3²×5。約数は 2^a·3^b·5^c（a＝0〜2, b＝0〜2, c＝0〜1）なので 3×3×2＝18（個）。',
    [('4個（指数をそのままかけた 2×2×1）', '指数 0 の場合を落としている。(2+1)(2+1)(1+1) で数える。'),
     ('9個（素因数 5 を数え忘れた値）', '180＝2²×3²×5 なので 5 の 0 乗・1 乗の 2 通りもかける。'),
     ('6個（指数に1を足して「足した」値）', '(a+1)(b+1)(c+1) は「かける」。3＋3＋2 ではない。')],
    unit='個', check=lambda: len(_d180) == 18, bp='180 の正の約数は何個あるか。'),
  S(2, '（2）', '正の約数の総和を求めよ。', 546,
    '(1＋2＋2²)(1＋3＋3²)(1＋5)＝7×13×6＝546。',
    [('455（5 の因数を (1＋5) でなく 5 とした値）', '指数 0 の項 1 を入れて (1＋5) とする。7×13×5 ではない。'),
     ('545（1 を足し忘れた値）', '約数には 1 も含まれる。'),
     ('366（180＋…と途中まで足した値）', '総和は (1+2+4)(1+3+9)(1+5) で漏れなく計算する。')],
    check=lambda: sum(_d180) == 546, bp='180 の正の約数の総和を求めよ。')],
 'N＝p^a q^b … のとき、約数の個数は (a+1)(b+1)…、総和は (1+p+…+p^a)(1+q+…+q^b)…')

# ============================================================
# ia5_2 順列
# ============================================================
PROB(91, 'ia5_2', '階乗・P・C を計算する',
 '次の計算をせよ。',
 [S(1, '（1）', '7! − 5!', 4920, '7!＝5040、5!＝120 なので 5040−120＝4920。',
    [('5160（7!＋5! と足した値）', '引き算なので 5040−120。足すのではない。'),
     ('42（7×6 と約分した値）', '引き算なので約分はできない。割り算 7!/5! と混同している。'),
     ('5040（5! を引き忘れた値）', '7!＝5040 から 5!＝120 を引く。')],
    check=lambda: fact(7) - fact(5) == 4920, bp='7! − 5! を計算せよ。'),
  S(2, '（2）', '9! ÷ 6!', 504, '9!/6!＝9×8×7＝504。',
    [('6（(9÷6)… と約分を誤った値）', '階乗の割り算は 9×8×7×6!/6! で 6! が消える。'),
     ('3024（9×8×7×6 まで掛けた値）', '6! で割ると 6 以下はすべて消える。9×8×7 で止める。'),
     ('72（9×8 で止めた値）', '9!/6! は 9, 8, 7 の 3 つをかける。')],
    check=lambda: fact(9) // fact(6) == 504, bp='9! ÷ 6! を計算せよ。'),
  S(3, '（3）', '₈P₃', 336, '₈P₃＝8×7×6＝336。',
    [('56（₈C₃ を計算した値）', 'P は並べるので 3! で割らない。'),
     ('512（8³ を計算した値）', '8³ は重複順列。P は 1 つずつ減らしてかける。'),
     ('40320（8! まで計算した値）', '₈P₃ は 8 から 3 個だけかける。')],
    check=lambda: perm(8, 3) == 336, bp='₈P₃ を計算せよ。'),
  S(4, '（4）', '₇C₅', 21, '₇C₅＝₇C₂＝(7×6)/(2×1)＝21。',
    [('2520（₇P₅ を計算した値）', 'C は選ぶだけなので 5! で割る。'),
     ('35（₇C₃ を計算した値）', '₇C₅＝₇C₂ であり ₇C₃ ではない。7−5＝2。'),
     ('42（7×6 で止め 2! で割り忘れた値）', '₇C₂＝7×6÷2 まで計算する。')],
    check=lambda: comb(7, 5) == 21, bp='₇C₅ を計算せよ。'),
  S(5, '（5）', '₁₂C₉ ＝ ₁₂Cᵣ（r ＜ 9）となる r を求めよ。', 3,
    'ₙCᵣ＝ₙCₙ₋ᵣ より ₁₂C₉＝₁₂C₁₂₋₉＝₁₂C₃。r＝3。',
    [('r＝4', '₁₂C₉ と等しいのは 12−9＝3 の ₁₂C₃。'),
     ('r＝1', '₁₂C₁＝12 で ₁₂C₉＝220 と一致しない。n−r を計算する。'),
     ('r＝6', '₁₂C₆＝924 で ₁₂C₉ より大きい。n−r＝3 になる。')],
    accepted=['r=3', 'r = 3'], check=lambda: comb(12, 9) == comb(12, 3),
    bp='₁₂C₉ ＝ ₁₂Cᵣ（r ＜ 9）が成り立つとき、r はいくつか。'),
  S(6, '（6）', '₉C₄ ＝ ₈C₃ ＋ ₈Cₖ が成り立つ k を求めよ。', 4,
    'ₙCᵣ＝ₙ₋₁Cᵣ₋₁＋ₙ₋₁Cᵣ（特定の 1 個を含む選び方＋含まない選び方）より ₉C₄＝₈C₃＋₈C₄。k＝4。',
    [('k＝5', '₈C₅＝₈C₃＝56 なので 56＋56＝112 となり ₉C₄＝126 にならない。'),
     ('k＝2', '₈C₃＋₈C₂＝84 で ₉C₃ に等しい。₉C₄ ではない。'),
     ('k＝3', '₈C₃＋₈C₃＝112 で ₉C₄＝126 と一致しない。')],
    accepted=['k=4', 'k = 4'], check=lambda: comb(9, 4) == comb(8, 3) + comb(8, 4),
    bp='₉C₄ ＝ ₈C₃ ＋ ₈Cₖ が成り立つとき、k はいくつか。')],
 'n!＝n(n−1)…1、ₙPᵣ＝n!/(n−r)!、ₙCᵣ＝n!/{r!(n−r)!}。ₙCᵣ＝ₙCₙ₋ᵣ と ₙCᵣ＝ₙ₋₁Cᵣ₋₁＋ₙ₋₁Cᵣ を使いこなす。')

_ps = [''.join(p) for p in itertools.permutations('picture')]
def _c(f): return sum(1 for s in _ps if f(s))
PROB(92, 'ia5_2', '両端・となり・順序を指定した並べ方',
 'picture の7文字をすべて並べて順列をつくる。次のようなものは何通りあるか。',
 [S(1, '（1）', 'p と e が両端にあるもの', 240,
    '両端の p, e の並べ方が 2 通り、間の 5 文字の並べ方が 5! 通り。2×120＝240（通り）。',
    [('120通り（両端の入れかえを忘れた値）', 'p…e と e…p の 2 通りがある。'),
     ('1440通り（p, e を1文字にまとめた値）', '「両端」は「となりあう」ではない。まとめてはいけない。'),
     ('480通り（両端の入れかえを4通りとした値）', '2 文字の並べ方は 2!＝2 通り。')],
    unit='通り', check=lambda: _c(lambda s: {s[0], s[-1]} == {'p', 'e'}) == 240),
  S(2, '（2）', 'c, t, u がとなりあっているもの', 720,
    'c, t, u をまとめて 1 文字とみると 5 個の並べ方 5!＝120 通り。まとまりの中の並べ方が 3!＝6 通り。120×6＝720（通り）。',
    [('120通り（まとまりの中の並べ方を忘れた値）', 'まとめた 3 文字の中でも 3! 通りの並べ方がある。'),
     ('4320通り（まとめた後も 6! で並べた値）', '3 文字を 1 つにまとめると並べるものは 5 個になる。'),
     ('240通り（中の並べ方を 2 通りとした値）', '3 文字の並べ方は 3!＝6 通り。')],
    unit='通り', check=lambda: _c(lambda s: max(s.index(ch) for ch in 'ctu') - min(s.index(ch) for ch in 'ctu') == 2) == 720),
  S(3, '（3）', 'p と i がとなりあっていないもの', 3600,
    'p, i 以外の 5 文字を並べて 5!＝120 通り。その両端と間の 6 か所から 2 か所を選んで p, i を入れる：₆P₂＝30 通り。120×30＝3600（通り）。（全体 7!−となりあう 2×6!＝5040−1440 でもよい）',
    [('1440通り（となりあう方を答えた値）', '1440 は p, i がとなりあう並べ方。全体 5040 から引く。'),
     ('1800通り（₆C₂ で入れる場所を選んだ値）', 'p と i は区別があるので ₆P₂＝30 通り。'),
     ('4320通り（7!−6! と計算した値）', 'となりあう並べ方は p, i の入れかえを含めて 2×6!。')],
    unit='通り', check=lambda: _c(lambda s: abs(s.index('p') - s.index('i')) != 1) == 3600),
  S(4, '（4）', 'i, c, u がこの順に並んでいるもの', 840,
    '7 か所から i, c, u を置く 3 か所を選べば並び方は 1 通りに決まる：₇C₃＝35 通り。残り 4 文字の並べ方 4!＝24 通り。35×24＝840（通り）。（7!÷3! でもよい）',
    [('5040通り（順序の条件を無視した 7!）', '3 文字の並びは 3! 通りのうち 1 通りだけなので 3! で割る。'),
     ('210通り（₇P₃ だけ数えた値）', '場所を選んだら順序は決まるので ₇C₃。さらに残り 4 文字の 4! をかける。'),
     ('120通り（i, c, u をとなりあわせた値）', '「この順」は「となりあう」ではない。間に他の文字が入ってよい。')],
    unit='通り', check=lambda: _c(lambda s: s.index('i') < s.index('c') < s.index('u')) == 840),
  S(5, '（5）', 'p が u より左にあり、e が u より右にあるもの', 840,
    'p, u, e を置く 3 か所を選べば並びは p, u, e の 1 通りに決まるので、₇C₃×4!＝840（通り）。',
    [('1680通り（p と e の左右を区別しなかった値）', 'p が左、e が右と決まっているので 3 文字の並びは 1 通り。'),
     ('2520通り（u の位置の条件だけで 2 で割った値）', 'p, u, e の 3 文字の順が指定されているので 7!÷3!。'),
     ('280通り（残り 4 文字の並べ方を 8 通りとした値）', '残り 4 文字の並べ方は 4!＝24 通り。')],
    unit='通り', check=lambda: _c(lambda s: s.index('p') < s.index('u') < s.index('e')) == 840)],
 'となり→まとめて 1 つ、となりあわない→他を並べて間・両端に入れる、順序指定→場所を組合せで選ぶ。')

_n96 = list(itertools.permutations(range(1, 8), 3))
_v = lambda t: t[0]*100 + t[1]*10 + t[2]
_tri = [c for c in itertools.combinations(range(1, 8), 3) if sum(c) % 3 == 0]
_six = sum(2*len([v for v in c if v % 2 == 0]) for c in _tri)
PROB(93, 'ia5_2', '倍数の見分け方で数える',
 '1 から 7 までの数字が1つずつ書かれた7枚のカードから3枚を選んで並べ、3桁の整数をつくる。次のような整数はいくつあるか。',
 [S(1, '（1）', '2 の倍数', 90,
    '一の位が 2, 4, 6 の 3 通り。残り 6 枚から百・十の位を並べて ₆P₂＝30 通り。3×30＝90（個）。',
    [('105個（全体 210 の半分とした値）', '1〜7 に偶数は 3 個しかないので半分にはならない。'),
     ('120個（一の位を 4 通りとした値）', '偶数は 2, 4, 6 の 3 つ。'),
     ('45個（百・十の位を ₆C₂ で選んだ値）', '百の位と十の位は区別があるので ₆P₂＝30 通り。')],
    unit='個', check=lambda: sum(1 for t in _n96 if _v(t) % 2 == 0) == 90),
  S(2, '（2）', '3 の倍数', 78,
    '和が 3 の倍数になる 3 数の組は ' + '、'.join('{' + ','.join(map(str, c)) + '}' for c in _tri) + f' の {len(_tri)} 組。各組の並べ方は 3!＝6 通りなので {len(_tri)}×6＝78（個）。',
    [('70個（全体 210 の 1/3 とした値）', '3 の倍数になる組を実際に数える必要がある。'),
     ('13個（組の数を答えた値）', '各組の 3 数の並べ方 3!＝6 通りをかける。'),
     ('66個（組を 11 組と数え落とした値）', '和が 9, 12, 15, 18 になる組も漏れなく数える。')],
    unit='個', check=lambda: sum(1 for t in _n96 if _v(t) % 3 == 0) == 78 == len(_tri) * 6),
  S(3, '（3）', '4 の倍数', 50,
    '下 2 桁が 4 の倍数：12, 16, 24, 32, 36, 52, 56, 64, 72, 76 の 10 通り。百の位は残り 5 枚から 5 通り。10×5＝50（個）。',
    [('90個（一の位が偶数とした値）', '4 の倍数は下 2 桁で判定する。一の位が偶数は 2 の倍数の条件。'),
     ('40個（下 2 桁を 8 通りと数え落とした値）', '52, 56 など十の位が 5 のものも忘れない。'),
     ('60個（百の位を 6 通りとした値）', '下 2 桁で 2 枚使うので百の位は残り 5 枚。')],
    unit='個', check=lambda: sum(1 for t in _n96 if _v(t) % 4 == 0) == 50),
  S(4, '（4）', '6 の倍数', 34,
    '(2) の 13 組のうち、一の位に偶数を置く。組に含まれる偶数の個数×（残り 2 つの並べ方 2 通り）を足して 34（個）。',
    [('26個（3の倍数 78 の 1/3 とした値）', '6 の倍数は「3 の倍数かつ一の位が偶数」。組ごとに偶数の数を数える。'),
     ('39個（3の倍数 78 の半分とした値）', '組によって含まれる偶数の数が違うので半分にはならない。'),
     ('45個（2の倍数 90 の半分とした値）', '3 の倍数の条件も同時に満たすものだけを数える。')],
    unit='個', check=lambda: sum(1 for t in _n96 if _v(t) % 6 == 0) == 34 == _six)],
 '2 の倍数：一の位が偶数／3 の倍数：各位の和が 3 の倍数／4 の倍数：下 2 桁が 4 の倍数／6 の倍数：2 の倍数かつ 3 の倍数。')
for _s in BATTLE[-1]['subs']:
    _s['bp'] = f'1〜7 の数字が1つずつ書かれた7枚のカードから3枚を並べて3桁の整数をつくる。{_s["q"]}はいくつあるか。'

PROB(94, 'ia5_2', '同じ文字を含む並べ方',
 'success の7個の文字を横一列に並べるとき、並べ方は何通りあるか。',
 [S(1, '答', None, 420,
    '7 文字のうち s が 3 個、c が 2 個あるので 7!/(3!2!)＝5040/12＝420（通り）。',
    [('5040通り（同じ文字を区別した 7!）', '同じ s 同士、c 同士を入れかえても同じ並びなので割る。'),
     ('840通り（s の 3! だけで割った値）', 'c も 2 個あるので 2! でも割る。'),
     ('2520通り（c の 2! だけで割った値）', 's が 3 個あるので 3! でも割る。')],
    unit='通り', check=lambda: len(set(itertools.permutations('success'))) == 420)],
 '同じものが p 個、q 個…あるとき、並べ方は n!/(p!q!…)。')

def _circ(cond):
    return sum(1 for p in itertools.permutations(range(1, 8)) if cond((0,) + p))
PROB(95, 'ia5_2', '円卓の座り方',
 '両親とその子ども6人の計8人が円卓を囲んで座る。',
 [S(1, '（1）', '座り方は全部で何通りあるか。', 5040,
    '8 人の円順列なので (8−1)!＝7!＝5040（通り）。',
    [('40320通り（一列に並べた 8!）', '円卓は回転して同じになる並びを 1 つとみるので (8−1)!。'),
     ('2520通り（裏返しも同じとみた値）', '裏返しを同じとみるのはじゅず順列。円卓は区別する。'),
     ('720通り（6! で止めた値）', '8 人の円順列は 7!。')],
    unit='通り', check=lambda: _circ(lambda s: True) == 5040),
  S(2, '（2）', '両親が向かいあって座る方法は何通りあるか。', 720,
    '父の位置を固定すると母の位置は真向かいの 1 つに決まる。残り 6 席に子ども 6 人を並べて 6!＝720（通り）。',
    [('1440通り（両親の入れかえで2倍した値）', '父を固定した時点で回転の重複は消えている。さらに 2 倍しない。'),
     ('5040通り（条件を使わずに数えた値）', '母の位置が 1 つに決まる分だけ少なくなる。'),
     ('120通り（子どもを円順列 5! で並べた値）', '父を固定したので子どもは残り 6 席に一列と同じく並べる。')],
    unit='通り', check=lambda: _circ(lambda s: abs(s.index(0) - s.index(1)) == 4) == 720),
  S(3, '（3）', '両親がとなりあって座る方法は何通りあるか。', 1440,
    '両親をまとめて 1 人とみると 7 人の円順列で 6!＝720 通り。両親の入れかえが 2 通りあるので 720×2＝1440（通り）。',
    [('720通り（両親の入れかえを忘れた値）', '父母の左右の入れかえ 2 通りをかける。'),
     ('10080通り（まとめた後も一列 7! とした値）', 'まとめた 7 人も円に並べるので (7−1)!。'),
     ('2880通り（入れかえを4通りとした値）', '2 人の並べ方は 2!＝2 通り。')],
    unit='通り', check=lambda: _circ(lambda s: (s.index(0) - s.index(1)) % 8 in (1, 7)) == 1440)],
 '円順列は 1 つを固定して、残りを一列に並べると考える。')

_st = list(itertools.product('ABC', repeat=6))
PROB(96, 'ia5_2', 'くり返し選べるときの押し方',
 '1 から 6 までの整数を書いた6枚のカードそれぞれに、A, B, C の3種類のスタンプのうち1つを押す。',
 [S(1, '（1）', '使わないスタンプがあってもよいとき、押し方は何通りあるか。', 729,
    'どのカードも 3 通りずつなので 3⁶＝729（通り）。',
    [('216通り（6³ と計算した値）', 'カード 6 枚それぞれがスタンプ 3 通りを選ぶので 3⁶。'),
     ('120通り（₆P₃ と計算した値）', '同じスタンプを何枚にも押してよいので重複順列。'),
     ('18通り（3×6 と計算した値）', '各カードの 3 通りを 6 回「かける」。')],
    unit='通り', check=lambda: len(_st) == 729),
  S(2, '（2）', '使わないスタンプがちょうど1種類になる押し方は何通りあるか。', 186,
    '使う 2 種類の選び方 ₃C₂＝3 通り。その 2 種類での押し方 2⁶＝64 のうち 1 種類だけの 2 通りを除いて 62 通り。3×62＝186（通り）。',
    [('192通り（1種類だけの場合を引き忘れた値）', '2⁶ の中には全部 A、全部 B のように 1 種類だけのものが含まれる。'),
     ('189通り（1種類だけを 1 通りしか引かなかった値）', '2 種類それぞれについて「そればかり」の 2 通りを引く。'),
     ('62通り（使う2種類の選び方をかけ忘れた値）', 'どの 2 種類を使うかで ₃C₂＝3 通りある。')],
    unit='通り', check=lambda: sum(1 for s in _st if len(set(s)) == 2) == 186),
  S(3, '（3）', '3種類すべてのスタンプを使う押し方は何通りあるか。', 540,
    '全体 729 から、1 種類だけの 3 通りと (2) の 186 通りを引いて 729−3−186＝540（通り）。',
    [('543通り（1種類だけの 3 通りを引き忘れた値）', '全部同じスタンプの 3 通りも除く。'),
     ('120通り（₆P₃ と考えた値）', 'カードは 6 枚あり、同じスタンプを何枚にも押す。'),
     ('537通り（1種類だけを 6 通りとした値）', '全部 A・全部 B・全部 C の 3 通り。')],
    unit='通り', check=lambda: sum(1 for s in _st if len(set(s)) == 3) == 540)],
 '重複順列は nʳ。「ちょうど○種類」は、それより少ない種類の場合を引くのを忘れない。')
